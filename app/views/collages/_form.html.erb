<%= form_for(@collage) do |f| %>
  <% if @collage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collage.errors.count, "error") %> prohibited this collage from being saved:</h2>

      <ul>
      <% @collage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
